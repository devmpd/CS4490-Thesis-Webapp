import { AdditionalMetadata } from './additional-metadata';

describe('AdditionalMetadata', () => {
  it('should create an instance', () => {
    expect(new AdditionalMetadata()).toBeTruthy();
  });
});
